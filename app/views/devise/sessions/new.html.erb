<main role="main">
  <div class="text-center album py-5">
    <div class="container col-4">
      <%= image_tag("logo.png", :alt => "logo", :class => "mb-5 form-logo") %>
      <p class="lead text-muted"><small>
        새로운 계정 생성을 하시려면 하단의 <strong>회원 가입</strong>을 클릭해주세요!
      </small></p>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email, :input_html => { :class => 'form-control mb-1' }, placeholder:'Email', required: true, autofocus: true,  label: false %>
        <%= f.input :password, :input_html => { :class => 'form-control mb-3' }, placeholder:'Password', required: true, label: false %>
        <%= f.input :remember_me, label: '　로그인 정보 기억하기', :class => "form-control mb-5", as: :boolean if devise_mapping.rememberable? %>
      </div>
      <div class="form-actions">
      <%= f.button :submit, "로그인", :class => "btn btn-lg btn-success btn-block" %> <br>
      </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</main>
    <footer class="footer">
      <div class="container text-center">
        <span class="text-muted">LikeLion 6th 2018 Newbiethon</span><br>
        <span class="text-muted">가정의달 5팀 이민아 강연우 이혜진 김세윤 한현수 조민국</span>
      </div>
    </footer>